int main()
{
	int p = 2; // Двійкова система чисельності
	int num1 = 1, num2 = 0, num3 = 1, num4 = 1; // Введіть число у двійковій системі числення
	int dec = num4 + num3 * p + num2 * p * p + num1 * p * p *p; // Формула для переведення в десяткову систему числення

	return 0;
}
